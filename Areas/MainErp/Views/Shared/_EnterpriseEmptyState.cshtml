@{
    var title = Convert.ToString(ViewData["Title"] ?? "لا توجد بيانات");
    var description = Convert.ToString(ViewData["Description"] ?? "لا توجد نتائج مطابقة للبحث الحالي.");
}
<div class="erp-empty">
    <strong>@title</strong>
    <p>@description</p>
</div>
