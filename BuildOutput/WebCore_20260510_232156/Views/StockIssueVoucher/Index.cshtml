@model IEnumerable<MyERP.Models.StockIssueVoucher>

@{
    UICulture = Session["lang"] != null ? Session["lang"].ToString() : "ar";

    if (ViewBag.Type == 1)
    {
        ViewBag.Title = MyERP.Properties.Resources.WarehouseExchangeVouchers;
    }
    else if (ViewBag.Type == 2)
    {
        ViewBag.Title = MyERP.Properties.Resources.CorruptedVouchers;
    }
    else if (ViewBag.Type == 3)
    {
        ViewBag.Title = MyERP.Properties.Resources.LostVouchers;
    }
}

<div class="row">
    <div class="col-12">
        <div class="page-title-box">
            <div class="page-title-right">
                <a class="btn btn-info" href="@Url.Action("AddEdit",new { type=ViewBag.Type})" role="button">@MyERP.Properties.Resources.AddNew</a>
            </div>
            <h4 class="page-title">@ViewBag.Title</h4>
        </div>
    </div>
</div>
<div class="row">
    <div class="col-12">
        <div class="card-box">
            <div class="row">
                <div class="col-12">
                    <div class="form-horizontal" role="form">
                        <div class="form-group row">
                            <div class="col-xs-12 col-sm-6 col-lg-3">
                                <input type="text" class="form-control searchtxt" value="@Html.Raw(ViewBag.searchWord)" placeholder=@MyERP.Properties.Resources.Search>
                            </div>
                            <div class="col-xs-12 col-sm-3 col-lg-3">
                                <button type="button" class="btn btn-success btnSearch" onclick="searchWord = $('.searchtxt').val(); paging();">
                                    <i class="fa fa-search"></i>
                                </button>
                                <a class="btn btn-info" href="@Url.Action("Index")" role="button"><i class="fe-refresh-ccw"></i></a>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
            <div class="row">
                <div class="col-12">
                    <div class="table-responsive">
                        <table class="table table-striped table-centered mb-0">
                            <thead>
                                <tr>
                                    <th>
                                        @MyERP.Properties.Resources.DocNo
                                    </th>
                                    <th>
                                        @MyERP.Properties.Resources.Date
                                    </th>
                                    <th>
                                        @MyERP.Properties.Resources.MainDoc
                                    </th>
                                    @if ((bool)ViewBag.ShowCost)
                                    {
                                        <th>
                                            @MyERP.Properties.Resources.TotalCost
                                        </th>
                                    }
                                    <th>
                                        @MyERP.Properties.Resources.WareHouse
                                    </th>
                                    <th>
                                        @MyERP.Properties.Resources.Edit
                                    </th>
                                    @if (ViewBag.ShowSerialNumbers)
                                    {
                                        <th>
                                            @MyERP.Properties.Resources.SerialNo
                                        </th>
                                    }
                                    <th>
                                        @MyERP.Properties.Resources.Delete
                                    </th>
                                    <th>@MyERP.Properties.Resources.Print</th>
                                </tr>
                            </thead>
                            @foreach (var item in Model)
                            {
                        <tr>
                            <td>
                                @*@Html.DisplayFor(modelItem => item.DocumentNumber)*@
                                @Html.Raw(item.DocumentNumber.ToString().Replace("-", ""))
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.VoucherDate)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.SystemPage.ArName)
                            </td>
                            @if ((bool)ViewBag.ShowCost)
                            {
                                <td>
                                    @Html.DisplayFor(modelItem => item.TotalCostPrice)
                                </td>
                            }
                            <td>
                                @Html.DisplayFor(modelItem => item.Warehouse.ArName)
                            </td>
                            <td>
                                <a href="@Url.Action("AddEdit","StockIssueVoucher",new { id=item.Id , type = ViewBag.Type})" onclick="$('#preloader, #status').fadeIn();">
                                    <i class="fa fa-edit"></i>
                                </a>
                            </td>

                            @if (item.SystemPageId != null && ViewBag.ShowSerialNumbers)
                            {
                                <td>
                                    <a class="btn btn-success" target="_blank" href="@Url.Action("AddEditSerialNumber", "StockIssueVoucher", new { id = item.Id, itemId = 0 })" role="button">@MyERP.Properties.Resources.SerialNo</a>
                                </td>
                            }
                                <td>
                                    @if (item.AutoCreated == true)
                                    {
                                        <a href="javascript:void(0);" onclick="Swal.fire({
                                        title: '@MyERP.Properties.Resources.TheAutomaticDocumentCannotBeDeleted' + '!',
                                        text: ' ',
                                        icon: 'error',
                                        showCancelButton: false,
                                        confirmButtonText: '@MyERP.Properties.Resources.Ok',
                                        confirmButtonColor: '#3085d6'
                                    });">
                                            <i class="fas fa-trash-alt"></i>
                                        </a>
                                    }
                                    else
                                    {
                                        <a href="javascript:void(0);" onclick="DeleteFunc(@item.Id)">
                                            <i class="fas fa-trash-alt"></i>
                                        </a>
                                    }
                                </td>
                                <td>
                                    <a href="@Url.Action("StockIssueVoucher","Report",new { id=item.Id,print=true,showReport=true})" target="_blank">
                                        <i class="remixicon-printer-fill"></i>
                                    </a>
                                </td>
                            </tr>
                            }

                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<div style="text-align:center;margin-bottom: 50px;">

    <div class="form-group row">
        <div class="col-sm-4">
            <select id="ShowByPage" class="form-control" style="width:70px;float:left;" onchange="paging()">
                <option value="10">10</option>
                <option value="30">30</option>
                <option value="50">50</option>
                <option value="100">100</option>
                <option value="200">200</option>
                <option value="@ViewBag.Count">@MyERP.Properties.Resources.All</option>
            </select>
            <span style="font-size:12px;float:right;"> @MyERP.Properties.Resources.NoOfResults : <span id="RowsCount">@ViewBag.Count</span></span>
        </div>
        <div class="col-sm-2">
            <lablel class="col-sm-8"> @MyERP.Properties.Resources.PageNo: </lablel>
        </div>
        <div class="col-sm-2">
            <select id="pagination" class="pagination form-control" style="width:70px;float:left;">
            </select>
        </div>
    </div>
</div>

<script>
    var pageNo = 1;
    var searchWord = '@Html.Raw(ViewBag.searchWord)';
    $('#ShowByPage').val(@ViewBag.wantedRowsNo);

    function paging() {

        var link = '/StockIssueVoucher?type='+@ViewBag.Type + '&pageIndex=' + pageNo + '&wantedRowsNo=' + $('#ShowByPage').val() + '&searchWord=' + searchWord;
        window.location.replace(link);
    }

    function setPages() {
        var count =@ViewBag.Count;
        for (var i = 0; i < Math.ceil(count / $('#ShowByPage').val()); i++)
            $('#pagination').append('<option onchange="pageNo=' + (i + 1) + '; paging();" id="pg' + (i + 1) + '">' + (i + 1) + '</option>');
    }

    setPages();
   $('#pg' +@ViewBag.PageIndex).attr('selected', 'selected')

    $('#pagination').on('change', function () {
        pageNo = $('#pagination').val();
         paging();
    })



    function DeleteFunc(id) {


        Swal.fire({
            title: '@MyERP.Properties.Resources.AreYouSureYouWantToDelete',
            text: " ",
            icon: 'warning',
            showCancelButton: true,
            confirmButtonText: '@MyERP.Properties.Resources.Ok',
            cancelButtonText: '@MyERP.Properties.Resources.Cancel',
            confirmButtonColor: '#3085d6',
            cancelButtonColor: '#d33',
            reverseButtons: true
        }).then((result) => {
            if (result.isConfirmed) {
                $('#preloader, #status').fadeIn();
                $.ajax(
                    {
                        url: '/StockIssueVoucher/Delete',
                        type: 'POST',
                        data: {
                            "Id": id,
                        },
                        error: function (e) {
                            $('#preloader, #status').fadeOut();
                            // alert("@MyERP.Properties.Resources.DeleteDoesNotSucceeded");
                            Swal.fire(
                                {
                                    title: '@MyERP.Properties.Resources.DeleteDoesNotSucceeded',
                                    text: " ",
                                    icon: 'error',
                                    showCancelButton: false,
                                    confirmButtonText: '@MyERP.Properties.Resources.Ok',
                                    confirmButtonColor: '#3085d6'
                                });
                        },
                        success: function (r) {
                            if (r == 'true') {
                                window.location.reload();
                            } else if (r == 'autoCreated') {
                                // alert("@MyERP.Properties.Resources.TheAutomaticDocumentCannotBeDeleted" + "!");
                                Swal.fire(
                                    {
                                        title: "@MyERP.Properties.Resources.TheAutomaticDocumentCannotBeDeleted" + "!",
                                        text: " ",
                                        icon: 'error',
                                        showCancelButton: false,
                                        confirmButtonText: '@MyERP.Properties.Resources.Ok',
                                        confirmButtonColor: '#3085d6'
                                    });
                                $('#preloader, #status').fadeOut();
                            } else
                                // alert("@MyERP.Properties.Resources.DeleteDoesNotSucceeded");
                                Swal.fire(
                                    {
                                        title: "@MyERP.Properties.Resources.DeleteDoesNotSucceeded",
                                        text: " ",
                                        icon: 'error',
                                        showCancelButton: false,
                                        confirmButtonText: '@MyERP.Properties.Resources.Ok',
                                        confirmButtonColor: '#3085d6'
                                    });
                            $('#preloader, #status').fadeOut();
                        }
                    });
            }
            else {
                result.dismiss;
            }
        });

        @*if (confirm('@MyERP.Properties.Resources.AreYouSureYouWantToDelete')) {
            $('#preloader, #status').fadeIn();
            $.ajax(
                {
                    url: '/StockIssueVoucher/Delete',
                    type: 'POST',
                    data: {
                        "Id": id,
                    },
                    error: function (e) {
                        $('#preloader, #status').fadeOut();
                        alert("@MyERP.Properties.Resources.DeleteDoesNotSucceeded");
                    },
                    success: function (r) {
                        if (r == 'true') {
                            window.location.reload();
                        } else if (r == 'autoCreated') {
                            alert("@MyERP.Properties.Resources.TheAutomaticDocumentCannotBeDeleted"+"!");
                            $('#preloader, #status').fadeOut();
                        }else
                            alert("@MyERP.Properties.Resources.DeleteDoesNotSucceeded");
                        $('#preloader, #status').fadeOut();
                    }
                });
        }*@
    }
</script>
