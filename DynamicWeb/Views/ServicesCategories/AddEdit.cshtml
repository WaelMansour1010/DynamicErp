
<div class="panel panel-widget forms-panel">
    <div class="forms">
        <div class=" form-grids form-grids-right">
            <div class="widget-shadow " data-example-id="basic-forms">
                <div class="form-title">
                    <input type="button" class="btn btn-info btnAdd" name="AddEdit" value="رجوع" onclick="back('@ViewContext.RouteData.Values["controller"].ToString()')" style="float:left;">
                    <h4>بيانات نوع الخدمة :</h4>
                </div>
                <div class="form-body">
                    <form class="form-horizontal">

                        <div class="form-group">
                            <label for="inputtext3" class="col-sm-3 control-label">الاسم عربي</label>
                            <div class="col-sm-9">
                                <input type="text" class="form-control" name="txtArName" id="txtArName" placeholder="الاسم العربى" required>
                                <span class="Asterisks">*</span>
                            </div>

                        </div>

                        <div class="form-group">
                            <label for="inputtext3" class="col-sm-3 control-label">الاسم انجليزى</label>
                            <div class="col-sm-9">
                                <input type="text" class="form-control" name="txtEnName" id="txtEnName" placeholder="الاسم انجليزى" required>
                
                            </div>

                        </div>
                        <div class="form-group">
                            <label for="inputtext3" class="col-sm-3 control-label">النوع </label>
                            <div class="col-sm-9">

                                <select class="form-control" id="type">
                                    <option value="0">اختر النوع</option>
                                    <option value="1">رئيسي</option>
                                    <option value="2">وسيط</option>
                                    <option value="3">فرعى</option>

                                </select>
                            </div>
                        </div>
                        <div class="form-group">
                            <label for="inputtext3" class="col-sm-3 control-label">الفئة الرئيسية  </label>
                            <div class="col-sm-9">
                                <select id="DDLParent" name="DDLParent" class="form-control"></select>

                                              
                            </div>
                        </div>
      

                        <div class="col-sm-offset-2">
                            <button type="button" class="btn btn-success" onclick="save()">
                                حفظ
                            </button>
                            <button type="reset" class="btn btn-danger" onclick="back('@ViewContext.RouteData.Values["controller"].ToString()')">
                                رجوع
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

<input type="hidden" id="hdnobjID" />
<input type="hidden" id="hdnUserId" />
<input type="hidden" id="hdnIsDeleted" />
<input type="hidden" id="hdnIsActive" />
<input type="hidden" id="hdnImage" />
<script>
    
    $(document).on('change', '#type', function () {
        var id = $('#type').val();
        $('#DDLParent').empty();
        if (id != 1) {
            FillParentDDL();

        }
    });
    function save() {

        CheckInputs();

        if ($("form").valid() && $('.error:visible').length == 0) {
            $('#preloader, #status').fadeIn();
            $.ajax(
                {
                    url: MainDomain + 'api/ServicesCategories/' + $('#hdnobjID').val(),
                    type: ((@ViewBag.ID.ToString() != '') ? 'PUT' : 'POST'),
                    data: {
                        "Id": $('#hdnobjID').val(),

                        "ArName": $('#txtArName').val(),
                        "EnName": $('#txtEnName').val(),
                        "ParentId": $('#DDLParent').val(),
                        "type": $('#type').val(),
                        "UserId": $('#hdnUserId').val(),
                        "IsDeleted": (@ViewBag.ID.ToString() != '') ? $('#hdnIsDeleted').val() : false,
                        "IsActive": (@ViewBag.ID.ToString() != '') ? $('#hdnIsActive').val() : true,
                        "Image": (@ViewBag.ID.ToString() != '') ? $('#hdnImage').val() : true,
                    },
                    beforeSend: function (xhr) {
                        xhr.setRequestHeader('Authorization', 'Bearer ' + $("#userToken").val());
                    },
                    error: function (e) {
                        if (e) {

                            $('#preloader, #status').fadeOut();
                        } else
                            window.location = '/logout/';
                    },
                    success: function (r) {
                        back('@ViewContext.RouteData.Values["controller"].ToString()');
                    }
                });
        } else {
            CheckSave();
            $("label.error").html('يرحى ادخال هذا الحقل');
            $('html').animate({ scrollTop: $('.error:visible').offset().top - 170 }, '500', 'swing');
        }
    }

    if (@ViewBag.ID.ToString() != '') {

        $.ajax(
            {
                url: MainDomain + 'api/ServicesCategories/@ViewBag.ID.ToString()',
                type: 'Get',
                beforeSend: function (xhr) {
                    xhr.setRequestHeader('Authorization', 'Bearer ' + $("#userToken").val());
                },
                error: function (e) {
                    console.log(e);
                    window.location = '/logout/';
                },
                success: function (r) {

                    if (r != null) {

                        $('#txtArName').val(r['ArName']);
                        $('#txtEnName').val(r['EnName']);
                        $('#DDLParent').val(r['ParentId']);
                        $('#type').val(r['Type']);
                        $('#hdnImage').val(r['Image']);
                        $('#hdnobjID').val(r['Id']);
                        $('#hdnUserId').val(r['UserId']);
                        $('#hdnIsDeleted').val(r['IsDeleted']);
                        $('#hdnIsActive').val(r['IsActive']);

                        FillAutoCompelete();
                    }

                    $('#preloader, #status').fadeOut();
                }
            });
    }

</script>


