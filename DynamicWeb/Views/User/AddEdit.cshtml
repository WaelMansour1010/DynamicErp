
@model MyERP.Models.User
<script src="~/Scripts/SaveAndNew.js"></script>
<script src="~/js/DeleteFunctionForAddEditView.js"></script>
@{ ViewBag.title = "المستخدمين";}

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    <div class="panel panel-widget forms-panel">
        <div class="forms">
            <div class=" form-grids form-grids-right">
                <div class="widget-shadow " data-example-id="basic-forms">
                    <div class="form-title">
                        @if (Model.Id > 0)
                        {
                            if (Model.IsDeleted == false)
                            {
                                <a class="btn btn-info" href="@Url.Action("Index")" role="button" style="float:left;">رجوع</a>
                                if (ViewBag.Previous > 0)
                                {
                                    <a href="@Url.Action("AddEdit",new {id= ViewBag.First })" class="btn btn-info" >الأول</a>
                                    <a href="@Url.Action("AddEdit",new { id=ViewBag.Previous })" class=" btn btn-info" >&#8250;</a>
                                }
                                if (ViewBag.Next > 0)
                                {
                                    <a href="@Url.Action("AddEdit",new { id=ViewBag.Next })" class="btn btn-info" >&#8249;</a>
                                    <a href="@Url.Action("AddEdit",new {id=ViewBag.Last })" class="btn btn-info" >الأخير</a>
                                }
                            }
                        }
                        else
                        {<a class="btn btn-info" href="@Url.Action("Index")" role="button" style="float:left;">رجوع</a>}
                        <h4>بيانات المستخدم:</h4>
                    </div>

                    <div class="form-body">
                        <form class="form-horizontal " id="Form">
                            <input type="hidden" id="hdnobjID" value="@Model.Id" />
                            @Html.HiddenFor(model => model.IsActive)

                            <div class="form-group col-sm-12">
                                <label for="inputtext3" class="col-sm-2 control-label">الإسم</label>
                                <div class="col-sm-9">
                                    <input type="text" class="form-control" value="@Model.Name" name="Name" placeholder="الإسم" required>
                                    <span class="Asterisks">*</span>
                                </div>

                            </div>
                            <div class="form-group col-sm-12">
                                <label for="inputtext3" class="col-sm-2 control-label">إسم المستخدم </label>
                                <div class="col-sm-9">
                                    <input type="text" class="form-control" value="@Model.UserName" name="UserName" placeholder="اسم المستخدم ">

                                </div>

                            </div>
                            <div class="form-group col-sm-12">
                                <label for="inputtext3" class="col-sm-2 control-label">الرقم السرى </label>
                                <div class="col-sm-9">
                                    <input type="password" class="form-control" value="@Model.Password" name="Password" placeholder="الرقم السري ">

                                </div>
                            </div>
                            <input id="new" name="newBtn" hidden />
                            <div class="col-sm-offset-2">
                                @if (Model.Id == 0)
                                {
                                    <button type="submit" id="newbtn" class="btn btn-success">
                                        حفظ و جديد
                                    </button>
                                    <button type="submit" class="btn btn-success">
                                        حفظ
                                    </button>
                                    <a class="btn btn-danger" href="@Url.Action("Index")" role="button">رجوع</a>
                                }
                                else
                                {
                                    if (Model.IsDeleted == false)
                                    {
                                        <button type="submit" class="btn btn-success">
                                            حفظ
                                        </button>
                                        <a class="btn btn-danger" href="@Url.Action("Index")" role="button">رجوع</a>
                                        <a class="btn btn-danger" onclick="Delete(@Model.Id,'User')" role="button">حذف</a>

                                    }
                                }


                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
}



