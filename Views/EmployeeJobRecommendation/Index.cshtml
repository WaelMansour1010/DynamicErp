
@model IEnumerable<MyERP.Models.GetEmployeeJobRecommendation_Result>

@{
    if (System.Web.Configuration.WebConfigurationManager.AppSettings["JobApp"] == "true")
    {
        Layout = "~/Views/Shared/JobApp.cshtml";
    }
    /**/
    UICulture = Session["lang"] != null ? Session["lang"].ToString() : "ar";
    ViewBag.title = MyERP.Properties.Resources.EmployeesFollowUp;
}



<div class="row">
    <div class="col-12">
        <div class="page-title-box">
            @*<div class="page-title-right">
                    <a class="btn btn-info" href="@Url.Action("AddEdit")" role="button" style="float:left;">@MyERP.Properties.Resources.AddNew</a>
                </div>*@
            <div class="page-title"> @MyERP.Properties.Resources.EmployeesFollowUp</div>
        </div>
    </div>
</div>
<div class="row">
    <div class="col-12">
        <div class="card-box">
            <div class="row">
                <div class="col-12">
                    @*<div class="form-horizontal" role="form">
                        <div class="form-group row">
                            <label class="control-label">@MyERP.Properties.Resources.FromDate</label>
                            <div class="col-xs-12 col-sm-4 col-lg-3">
                                <input type="date" class="form-control" id="DateFrom">
                            </div>
                            <label class="control-label">@MyERP.Properties.Resources.ToDate</label>
                            <div class="col-xs-12 col-sm-4 col-lg-3">
                                <input type="date" class="form-control" id="DateTo">
                            </div>
                            <div class="col-xs-12 col-sm-2 col-lg-3">
                                <button type="button" class="btn btn-success btnSearch" onclick="searchWord = $('.searchtxt').val(); paging();">
                                    <i class="fa fa-search"></i>
                                </button>
                                <button type="button" class="btn btn-info btnSearch" onclick="pageNo = 1; searchWord=''; paging();">
                                    <i class="fe-refresh-ccw"></i>
                                </button>
                            </div>
                        </div>
                    </div>*@
                </div>
            </div>


            <div class="row">
                <div class="col-12">
                    <div class="table-responsive">
                        <table class="table table-striped table-centered mb-0">
                            <thead>
                                <tr>
                                    
                                    <th>@MyERP.Properties.Resources.Job</th>
                                    <th>@MyERP.Properties.Resources.Employee</th>
                                </tr>
                            </thead>
                            @foreach (var item in Model)
                            {
                                <tr>
                                    
                                    <td>
                                        @Html.DisplayFor(modelItem => item.JobArName)
                                    </td>
                                    
                                    <td>
                                        @Html.DisplayFor(modelItem => item.EmployeeArName)
                                    </td>
                                    @*<td>
                                        <a href="@Url.Action("AddEdit","CompanyJobRequest",new { id=item.CompanyJobRequestId })" onclick="$('#preloader, #status').fadeIn();">
                                            <i class="fa fa-eye"></i>
                                        </a>
                                    </td>
                                    <td>
                                        <a href="@Url.Action("AddEdit2","Employee",new { id=item.EmployeeId })" onclick="$('#preloader, #status').fadeIn();">
                                            <i class="fa fa-eye"></i>
                                        </a>
                                    </td>*@

                                </tr>
                            }

                        </table>
                    </div>
                </div>
            </div>

            <br />
            <div style="text-align:center;margin-bottom: 50px;">

                <div class="form-group row">
                    <div class="col-sm-4">
                        <select id="ShowByPage" class="form-control" style="width:70px;float:left;" onchange="paging()">
                            <option value="10">10</option>
                            <option value="30">30</option>
                            <option value="50">50</option>
                            <option value="100">100</option>
                            <option value="200">200</option>
                            <option value="@ViewBag.Count">@MyERP.Properties.Resources.All</option>
                        </select>
                        <span style="font-size:12px;float:right;"> @MyERP.Properties.Resources.NoOfResults : <span id="RowsCount">@ViewBag.Count</span></span>
                    </div>
                    <div class="col-sm-2">
                        <lablel class="col-sm-8"> @MyERP.Properties.Resources.PageNo: </lablel>
                    </div>
                    <div class="col-sm-2">
                        <select id="pagination" class="pagination form-control" style="width:70px;float:left;">
                        </select>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>


<script>
       var pageNo = 1;
    var searchWord = '@Html.Raw(ViewBag.searchWord)';

    $('#ShowByPage').val(@ViewBag.wantedRowsNo);

    function paging() {
        var link = '/JobFollowUp/Index?' + 'pageIndex=' + pageNo + '&wantedRowsNo=' + $('#ShowByPage').val() + '&searchWord=' + searchWord + '&dateFrom=' + $('#DateFrom').val() + '&dateTo=' + $('#DateTo').val();
        window.location.replace(link);
    }

    function setPages() {
        var count =@ViewBag.Count;
        for (var i = 0; i < Math.ceil(count / $('#ShowByPage').val()); i++)
        //    $('.pagination').append('<li class="pageNo" id="pg' + (i + 1) + '"><a href="javascript:void(0);" onclick="pageNo=' + (i + 1) + '; paging();">' + (i + 1) + '</a></li>');
     $('#pagination').append('<option onchange="pageNo=' + (i + 1) + '; paging();" id="pg' + (i + 1) + '">' + (i + 1) + '</option>');
    }

    setPages();
   $('#pg' +@ViewBag.PageIndex).attr('selected', 'selected')

    $('#pagination').on('change', function () {
        pageNo = $('#pagination').val();
         paging();

    })

  //  $('#pg' +ViewBag.PageIndex).addClass('disabled');
    @*function Delete(id) {
        if (confirm('@MyERP.Properties.Resources.AreYouSureYouWantToDelete')) {

            $('#preloader, #status').fadeIn();
            $.ajax(
                {
                    url: '/JobFollowUp/Delete',
                    type: 'POST',
                    data: {
                        "Id": id,
                    },
                    error: function (e) {

                    },
                    success: function (r) {
                        if (r == 'true') {
                            window.location.reload();
                        }
                    }
                });
        }
    }
    function ActivateDeactivate(id) {
        $.ajax({
            url: '/JobFollowUp/ActivateDeactivate',
            type: 'POST',
            data: {
                "Id": id,
            },
            error: function (e) {

            },
            success: function (r) {
                if (r == 'true') {
                    alert('@MyERP.Properties.Resources.TheActivationStateHasChanged')
                }
            }
        });
    }*@
</script>
